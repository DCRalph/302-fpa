import { Nunito, Montserrat, Cabin } from "next/font/google";

export const nunito = Nunito({subsets: ["latin"]});
export const montserrat = Montserrat({subsets: ["latin"]});
export const cabin = Cabin({subsets: ["latin"]});